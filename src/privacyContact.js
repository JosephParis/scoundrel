// The single place the privacy contact address is defined, so changing it is a
// one-line edit rather than a search across the policy and the modals.
//
// !! ACTION REQUIRED BEFORE LAUNCH !!
// This mailbox does not exist yet. A policy that lists an address nobody reads is
// worse than no policy: a deletion request would vanish silently. Create it (or
// swap in whichever address you prefer) and send a test message before inviting
// anyone. Tracked on issue 13's pre-launch checklist.
export const PRIVACY_CONTACT = 'scoundrel.privacy@gmail.com'
